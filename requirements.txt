# Brain Tumor MRI Detector — Python dependencies
# ──────────────────────────────────────────────
# Install:  pip install -r requirements.txt
# Docker:   used in Dockerfile.backend

# ── ML / Vision ───────────────────────────────
tensorflow>=2.13,<3.0
numpy>=1.24,<2.0
opencv-python-headless>=4.8   # headless = no GUI, smaller image
Pillow>=10.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
pandas>=2.0

# ── Web framework ─────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# ── Database / Auth ───────────────────────────
SQLAlchemy>=2.0
bcrypt>=4.2
PyJWT>=2.9

# ── Reporting ─────────────────────────────────
reportlab>=4.2

# ── Dev / Testing (not installed in Docker) ───
# To install only prod deps:  pip install -r requirements.txt
# To include test deps:       pip install -r requirements.txt -r requirements-dev.txt
